# Screenshots

This folder stores the current screenshots used by the main project README.

Active captures for the React + FastAPI version:

- `home-react.png`
- `predictor-react.png`
- `model-react.png`

Older screenshots from the removed Streamlit version are no longer part of the project.
