# Full environment for local development: model training, data scraping, notebooks.
# The deployed API only needs backend/requirements.txt (smaller, faster Render builds).
-r backend/requirements.txt
requests==2.32.3
beautifulsoup4==4.12.3
matplotlib==3.10.1
seaborn==0.13.2
pytest==8.3.4
httpx==0.28.1
