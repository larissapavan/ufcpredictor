This directory is reserved for backend-side data artifacts when the API needs runtime resources beyond the packaged model.

At the moment, the service loads fighter metadata from the main project dataset and model bundle stored in the repository.
